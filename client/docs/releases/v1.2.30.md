# Olivia 1.2.30

- 统一信件、QQ、微信、主动消息及语音、视频、歌曲内容生成的上下文编排，保留原文说话人、时间和来源。
- 事件解释同时携带人物、动作、时间、否定信息和阶段，避免把不同动作的完成状态混用。当前生活状态只读取已发布的世界记录。
- 修复召回结构约束未传给模型的问题；保留可核实的部分原文，不因单条引用无效丢弃全部证据。
- 历史消息按对话顺序进入生成请求，输出格式要求在上下文整理后统一设置；阻止会话推进后重发断线前的旧草稿。
- 沿用现有模型调用，不增加每轮回复的复核或重写调用。

## 验证与已知限制

统一上下文改动的本地回归 1909 项通过。真实 qwen3.7-flash 合成回放仍出现短句纠错误读、前后矛盾时编造解释及输出格式不稳定，本版不宣称这些语义问题已彻底解决；本地测试不等同于用户设备验收。

已有本地版可在补丁更新页安装本版，或手动导入 `.oliviapatch`。升级前可导出信件备份；校验文件随补丁提供。保留旧版补丁以便需要时回退，回退程序不会修正已生成的历史内容。
