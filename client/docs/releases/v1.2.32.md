# Olivia 1.2.32

- 修复自动选择语音回信时，模型的请求分类字段导致 `REPLY_ROUTE_INVALID_RESULT` 的兼容问题，保留媒体能力校验，不增加模型调用。
- 修复旧信延迟处理覆盖较新的 QQ／微信渠道选择；新增渠道不再移除已有渠道。
- 写信弹窗的信纸适配可用空间，短信不再出现多余的横向和纵向滚动条，长信仍可在输入区域滚动。
- QQ 聊天适当增加语音使用倾向，由模型结合语境选择，不强制比例；取消历史不便收听状态的持续限制。

语音路由修复已用实际服务上游的合成请求和失败响应回放验证。此版本不包含尚未完成的 QQ 启动依赖修复。
