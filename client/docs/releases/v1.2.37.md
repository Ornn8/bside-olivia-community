# Olivia 1.2.37

- 修复普通聊天、情绪倾诉、长聊、夸赞翻唱和取消唱歌时，可能因回信分类字段矛盾而出现 `REPLY_ROUTE_INVALID_RESULT`、无法寄信的问题。
- 简化回信分类：模型只填写必要决策，程序统一推导辅助字段并落实媒体能力限制，不增加模型调用或重试。
- 保留明确媒体请求、只要文字等约束，以及对未知类型、损坏数据和服务错误的检查。

验证：238 项相关回归测试通过；45 组历史返回回放通过；自有服务同一组 45 个路由样本真实重测全部通过。真实重测仅覆盖路由，不包含媒体生成或完整寄信。

已有用户可安装 `.oliviapatch` 补丁更新，无需重新安装程序。
