# Olivia 1.2.29

- 云端模式的旧视频设置入口改为查询服务器能力，不再检查本地生成模型或 GPU。
- 云端生成不再被无关的本地模型配置路径阻断。
- 云端翻唱允许留空歌词，由服务器在生成时识别；不再要求安装本地歌词识别组件。手动歌词、本地输入校验、下载校验及 FFmpeg 组装保留。
- OpenCode 返回地区授权错误时，明确提示到其 Go 账户页面授权中国托管模型，不再笼统显示连接失败。
