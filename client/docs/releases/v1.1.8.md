# Olivia 1.1.8 — 路由诊断与主动写信界面

- 路由诊断贯通网关、分类器、服务端日志和诊断包，保留安全的失败阶段、网关错误码、HTTP 状态和异常类型。
- 区分连接请求、响应 JSON、工具调用解析和路由校验；工具调用进一步区分缺失、名称或参数异常。前端连接、超时和响应格式错误也有独立错误码。
- 主动写信设置改为与主界面一致的暖灰色开关、说明和分隔线，保留原有保存行为与键盘操作。

在“补丁更新”中导入 `Olivia-1.1.8.oliviapatch`，完全退出并重新打开 Olivia。无需重下离线组件或清空记忆。若回信形式检测仍失败，请重现后导出新的诊断包。

本版增强定位能力，不代表未知的模型接口失败已被修复。不导出信件正文、模型原文或密钥。本地服务完全断开时，前端错误码仍会显示，但无法写入服务端诊断包。

相关本地检查通过，主动写信组件已独立渲染检查；未调用真实模型 API。仅发布补丁和校验文件。
