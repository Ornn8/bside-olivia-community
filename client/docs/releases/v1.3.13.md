# Olivia 1.3.13

- 修复 QQ 已登录后，后台短暂探测失败导致“连接并保存”仍报 `QQ_LOGIN_UNAVAILABLE` 的问题。
- 点击绑定时重新检查当前登录；检查通过后刷新组件状态并保存绑定，不再依赖过期的后台状态。
- 保留机器人账号一致性、个人 QQ 号和 OneBot 配置校验；真实未登录或配置未就绪时仍会提示错误。

已有用户下载 `Olivia-1.3.13.zip`，在“本地陪伴 → 补丁更新”中导入，完全退出并重新打开 Olivia，再尝试连接并保存 QQ。新安装或覆盖安装可使用同页 `Olivia-Setup-x64.exe`。

包含 1.3.12 的客户端启动、QQ 回复格式兼容、聊天入口开放和记忆组件识别修复。无需重新下载已有记忆离线包或清空用户数据。
