# Olivia 1.3.6

- 修复误选已有 install 目录后出现重复安装层级的问题，并让快捷方式使用安装器实际确认的目录。
- 已出现 install/install 且新目录没有对话记录时，启动自动读取原目录的信件、聊天、关系和密钥；两边数据均不搬动、不覆盖。如果两边均有对话则停止自动切换，保留双方记录。
- 更新补丁携带离线图片依赖，启动时自动检查并修复缺失的 Pillow，无需覆盖安装。
- 安装器验证图片编解码能力，避免安装成功后才发现照片无法生成。

在设置的本地更新中选择本版本 ZIP，安装完成后完全退出并重新打开 Olivia。无需卸载、重新安装或重新填写 Key。
