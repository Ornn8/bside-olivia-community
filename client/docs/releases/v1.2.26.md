# Olivia 1.2.26

- 回信服务集中管理模型调用与 Olivia 账户；账户页可获取、显示 Key，查看余额、调用量并创建充值订单。
- 充值显示专属付款金额，请使用群内公告中的微信二维码，严格按页面金额付款；匹配成功后按所选充值档位入账。
- 云端 GPU 支持音频与口型分卡调度；云端生成素材后交由本机组装，保留本地生成方式。
- R2 只用于生成素材临时中转，本机下载校验或组装成功后确认删除；清理失败会重试，不重复生成或收费。
- 改进后台结构化校验和稳定提示前缀，保留最新 Qwen Flash 路由兼容修复。

双 RTX 4090 已完成原生客户端生成、素材回传、本地组装及播放验收。云端 GPU 需要可用的服务地址与凭据；本补丁不包含模型权重或 GPU 服务部署。充值服务依赖收款监听在线。
