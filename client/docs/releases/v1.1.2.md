# Olivia 1.1.2

离线组件安装界面直接列出各组件的用途、对应 ZIP 文件名和安装状态，不再需要展开查找。按说话、唱歌和视频说明所需组件，统一使用多选 ZIP 入口，移除旧版整包导入及卸载入口。

已有用户可导入 `Olivia-1.1.2.oliviapatch` 后退出并重新打开程序；新安装使用 `Olivia-1.1.2-Setup-x64.exe`。已安装的模型无需重新下载。安装器包含基础媒体工具，补丁不携带 FFmpeg 或模型。

验证：隔离目录组件补丁安装、真实 tools 包导入、FFmpeg 启动及重复导入复用通过；界面取消、忙碌、失败及成功状态自动检查通过。此次未重新生成音视频，也未将记忆导入问题标记为已修复。
