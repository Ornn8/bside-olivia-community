# 林离世界交付清单

本清单区分用户确认的需求、已有实现和验收边界，不能将开发分支描述成已发布产品。

## 最新集成基线

- 当前工作分支：`codex/life-rhythm`。
- 已合入 PR381 的主线 `c3ab694` 与 PR382 的主线 `261b87de2a2f762f1462d2a1f70dd7331300ea96`。
- `0.1.455` 已被音色修复发行占用。后续包必须保留 Breeze CFG=1、RoFormer 双 stem → SoulX 转换 → 重混音 → LatentSync 及其下载清单，不能带回旧音色链路。
- 不操纵其他任务的端口 18999 隔离环境。

## 用户确认的需求

- [x] 对外模块改名“林离世界”，内部接口与数据库标识保持兼容。
- [x] 统一作息状态与用餐、休息、专注时段供回信和页面读取。
- [x] 按实际接收与最终回信完成时间累计睡眠窗口内的清醒时长。
- [x] 最后一次回信后安静满 30 分钟重新入睡；再来信算再次叫醒。连续通信与重复叫醒的影响不同，重复投递不重复计算。
- [x] 疲劳延续次日并可逐步恢复；不因为用户离线自动恶化。
- [x] 关系变化使用既有账本，不新增独立好感分数；夜间倾诉本身不扣关系。
- [x] 作息按稳定通信或明确且有时区依据的用户习惯渐进适应，保留睡眠时长，不回写旧夜晚。
- [x] 连续休息损失派生不适、自我照顾与逐步恢复，回信与页面共用状态；未实际恢复时不显示恢复中。
- [ ] 必要时咨询计划的真实模型验收：已有模拟与提示契约，不得自动补造就诊、疾病诊断或以不适索取补偿。
- [ ] 已约好夜聊、撤回约定、反复施压、尊重边界与关系修复：需补齐跨日真实模型验收，不能仅凭提示词存在宣称完成。
- [ ] 持续事项、主动分享及生活恢复与后续回信的长期连续性：需补齐多日真实信件验收。

## 已有证据，不扩大解释

- 旧四封 OpenCode Go / DeepSeek V4 Flash HTTP 信件虽然完成，但其记忆目录隔离配置无效：2026-09-06 的多日复测发现复用了工作树 `.olivia_data` 中旧测试记忆。撤销其“隔离合成用户端到端通过”结论，保留报告用于追溯，不计入发行成绩。
- 已修正验收脚本的 `OLIVIA_MEMORY_ROOT`、outbox 根目录与独立用户标识，并在发信前断言记忆、信箱、生活库均在本轮输出目录内。新的多日真实模型验收结果待完成后登记。
- 更名并合入 PR381 后，相关世界/人格/设置注入/控制中心回归为 800 passed；PR382 合版后同组重跑仍为 800 passed（1 条既有 aiohttp AppKey 提示）。后续功能仍应重新验证。
- 真实调用只选 `key.txt` 中明确标记 OpenCode 的 key，端点固定 OpenCode Go；禁止使用另一个官方 key，不将 key 写入报告。
- 新隔离多日轮 `rhythm-http-20260906-120836`：14/14 正式回信和生活提取完成，Mem0 outbox 14 条 written/duplicate 终态、pending=0，数据库重开一致。不适、减少练习、后续恢复和保留未兑现事项在回信中出现。但第3封为背景录音虚构了格言，不能称为无幻觉通过；此后已收紧出处边界，待最终复测。
- `rhythm-http-20260906-121817` 边界轮第4封出现 MEMORY_UNAVAILABLE，已与 BUG 任务协调诊断；未完成的边界轮不得计为成功。后续基于合版代码重新验收。
- 最终复测需覆盖刚修正的“真实休息后才进入恢复中”和出处边界，不能把修改前回信冒充修改后表现。

## 发布前待办

- [ ] 完成上述剩余行为与真实多日信件验收；保留失败样本及修复后的复测边界。
- [ ] 更新 README 和 `LINLI_WORLD.md`：重点写新增功能、具体测试与真实成绩、设计合理性，不把历史测试数写成最终成绩。
- [ ] 基于最新主线检查完整安装包与补丁包含新增模块及 0.1.455 manifest。
- [ ] 实际升级验证：旧信件、记忆、关系和生活记录保留；原生入口显示“林离世界”。
- [ ] 新目录完整安装验收和真实寄信联动验收。
- [ ] 对齐未占用版本号，发布安装包与升级补丁，提供实际产物和校验值。
