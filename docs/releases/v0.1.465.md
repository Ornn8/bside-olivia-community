# Olivia 0.1.465

补充长期记忆初始化诊断。原先只有 `MEM0_INITIALIZATION_FAILED` 的部分失败，现在会提供固定分类的失败步骤、组件和异常类型，例如后端构造、依赖加载、配置解析、存储目录准备、状态查询和旧实例关闭。

分类可区分向量存储、Embedding、LLM 客户端和 Mem0 内部调用。沿用已有状态接口和诊断包错误码通道，不导出异常正文、私人文件路径、密钥或服务地址。保留 0.1.464 的本地演奏导入与安装诊断功能。

这次更新用于定位根因，并不宣称已修复第 22 份诊断包中的未知初始化异常。请更新后重新准备长期记忆；如果仍失败，立即导出新诊断包。

提供补丁、新安装器和 SHA256 校验文件。安装器未签名，可能显示未知发布者。
