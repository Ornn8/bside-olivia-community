# Olivia 1.1.7 — 诊断补充

- 回信形式检测失败时，记录分类失败与请求结果；界面区分额度、认证、限流、超时、结果格式异常，并保留安全错误码。
- 历史记忆导入的关系初始化失败，区分查询、输入准备、模型请求、结果解析和关系写入；模型请求进一步区分额度、认证、限流和超时。
- 诊断包标记上述新增能力，不导出信件正文、模型原文或 API Key。

本次是诊断与提示增强，不代表已修复所有回信形式检测或关系初始化失败。请在“补丁更新”中导入 `.oliviapatch` 后完全退出并重新打开 Olivia，重现问题后导出新的诊断包。无需清空记忆或重下离线组件。

本地相关检查通过；无真实 API 调用。仅发布补丁和校验文件。
