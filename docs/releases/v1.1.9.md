Olivia 1.1.9 增加 DeepSeek 模型列表同步，并补充回信交付信息与风格约束。

- DeepSeek 官方接口可自动获取模型列表并下拉选择，支持刷新；保留当前选择，获取失败保留已有列表。同一服务切换模型可复用已保存的 Key。自定义接口仍可手动填写模型。
- V4 Pro 路由请求不再强制发送 tool_choice，与 V4 Flash 使用相同兼容处理。
- 语音正文明确获得本次语音交付信息，避免沿用过去“只能打字”的能力描述；不宣称媒体已经生成成功。
- 历史回信用于核对事实，不作为口吻范本；不把抒情、昵称和亲近表达直接判断为过度依赖或越界。未增加内容审核或二次生成。
- 信纸上的发信、回信时间增加小时和分钟。

更新方式：下载 Olivia-1.1.9.oliviapatch，在设置的“补丁更新”中导入，完成后退出并重新启动。已有离线组件无需重新下载。

本次仅发布补丁及两个校验文件，不包含安装器、模型或 FFmpeg。

验证：348 项相关本地回归通过；原版 0.0.9.627 资源副本上的时间格式接线、JavaScript 语法和重复应用检查通过。发布前另验证补丁隔离安装和版本读取。模型接口测试使用合成响应，未调用用户 Key；提示词回归确认交付信息及约束正确传入，不等于真实模型的每次语气均已人工验收。
