# Olivia 0.1.468

历史记忆允许正常出现“林离”这个名字，取消把名字本身视作角色身份错误的检查。保留第一人称要求与其他身份边界，并同步提取提示。

补齐 Breeze TTS 子进程的失败诊断：退出前记录脱敏阶段、异常分类及固定错误码，主进程在清理临时目录前保留这些字段，导出诊断包时仍按白名单过滤。不会导出原始报错文本、信件内容或本地路径。

包含 0.1.467 的北京时间上下文与安装组件诊断更新。
