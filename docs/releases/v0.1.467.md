# Olivia 0.1.467

包含 0.1.466 的视频回退错误码和 TTS 脱敏诊断改进。

视频组件启用失败时，设置页和诊断包显示具体受影响的语音组件，包括音色参考、Python 环境、模型和配置文件。保留文件校验和原子发布保护；这项改进用于定位用户机器的缺失或损坏文件。

回信的必选上下文明确提供林离所在上海的北京时间，同时保留 UTC 时间。生成与质量检查区分双方作息，结合最近回信保持连续，避免因用户道晚安就把她的早晨当作夜晚。
