# Olivia 1.1.5 — 记忆导入补丁

- 后台历史导入遇到长期记忆初始化时，显示“正在准备长期记忆”，就绪后自动继续；等待超时或初始化失败时给出明确提示。重复提交沿用同一任务。
- 修复读取记忆列表与导入重叠时可能误报来源去重不可用的问题。现在限时等待读取完成，再执行原有去重检查；真正超时或校验失败时仍停止导入。
- 诊断包新增记忆初始化等待阶段，便于区分尚未开始导入与写入失败。

仅发布 `Olivia-1.1.5.oliviapatch`。在“补丁更新”中导入后完全退出并重新打开 Olivia，再重试历史信件导入。无需清空记忆，也无需重新下载离线组件。

验证：187 项相关本地检查通过，覆盖等待、超时、取消、重复提交、记忆管理互斥及读取与写入竞争。未调用真实 API。已复现并修复一条产生 `MEM0_SOURCE_DEDUP_UNAVAILABLE` 的竞争路径；该错误还有其他可能原因，不能据此保证所有同码用户问题均已解决。
