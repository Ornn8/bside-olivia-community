Olivia 1.1.12 · 记忆导入诊断增强

历史信件导入失败时，诊断包增加记忆接口的固定错误分类：常见 HTTP 状态码、请求超时、连接失败，以及既有的返回内容格式错误。保留失败事件，避免重试中的“写入等待”状态遮住上一轮失败原因。不记录 Key、接口地址、信件内容或服务商原始错误消息。

本次是诊断增强，不表示已解决用户的 Qwen 记忆导入故障。更新后重试一次导入；若仍失败，请导出新的诊断包。包含 1.1.11 的 Qwen 回信分类兼容修复。

仅发布程序补丁及校验文件。在“本地陪伴 → 补丁更新”中导入 Olivia-1.1.12.oliviapatch，完成后完全退出并重新启动。无需清空记忆或重新下载离线组件。
