# Olivia 1.1.3

安装器现在解析复制客户端目录时被汇总的内部错误，区分文件占用、权限不足、磁盘空间不足、文件缺失及路径过长，并提供相应提示。诊断只记录错误数字和代码位置，不记录原始错误消息中的私人路径。

遇到 `SETUP_PATCH_UNEXPECTED_ERROR` 且日志定位到 `copy_official_runtime` 的用户，可使用本版安装器重试；若仍失败，请保留新的错误提示及安装日志。本次补齐诊断，不代表已经确认或修复所有复制失败原因。

- 新安装或安装失败重试：`Olivia-1.1.3-Setup-x64.exe`。
- 已有安装更新：`Olivia-1.1.3.oliviapatch`，更新后重新打开程序。正常使用 1.1.2 的用户不必为此重装。
- 已安装模型无需重新下载；安装器包含基础媒体工具，补丁不携带 FFmpeg 或模型。

验证：真实 copytree 汇总错误的模拟回归、安装诊断及离线核心检查共 64 项通过、1 项跳过。未重新生成音视频，未验证故障用户的实际机器。
