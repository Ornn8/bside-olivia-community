# Olivia 0.1.466

修复视频回退诊断信息缺失：诊断包保留媒体异常链中的底层错误码，能区分 TTS、口型输入和视频合成失败。

说话视频的 TTS 子进程失败时，记录退出码与脱敏错误分类，包括缺少 Python 模块、DLL 依赖、文件、CUDA 显存不足及超时。原始 stderr 不写入持久诊断日志或导出包。

此更新用于定位回退原因，不代表已经修复所有用户端模型运行故障。安装后重试失败操作，再导出诊断包。
